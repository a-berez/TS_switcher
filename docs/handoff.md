# Handoff: copy grid 4×2
Date: 2026-08-27

`1.0.0-beta.6` в манифестах и `build.py`. Кнопки копирования: два ряда (ТС / рейтинги), `--copy-cols` по числу кнопок в ряду, текущий хост тоже показывается.

## Verify

- на info при полной видимости — 4×2, кнопка `info` есть
- на elo — 4×2, кнопка `elo` есть
- options: выключить часть copy-хостов — ряды ТС/рейтинги сохраняются, колонки подстраиваются
- копирование с кнопки текущего хоста даёт URL текущей страницы
